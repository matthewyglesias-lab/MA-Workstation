{
  "name": "ipmg-medassist-static-webapp",
  "version": "5.45.0",
  "private": true,
  "description": "IPMG MedAssist UDS / Injection / Samples static web app for Azure Static Web Apps.",
  "scripts": {
    "start": "node scripts/static-server.js",
    "check": "node scripts/check-app.js",
    "test": "npm run check",
    "test:e2e": "playwright test",
    "test:ci": "npm run check && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
