{
  "name": "ipmg-medassist-static-webapp",
  "version": "6.0.0",
  "private": true,
  "description": "IPMG MedAssist UDS / Injection / Samples static web app for Azure Static Web Apps.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "start": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "typecheck": "tsc --noEmit",
    "check:static": "node scripts/check-app.js",
    "check": "npm run typecheck && npm run check:static",
    "test": "npm run test:unit",
    "test:unit": "vitest run",
    "coverage": "vitest run --coverage",
    "test:e2e": "npm run build && playwright test",
    "test:e2e:artifact": "playwright test",
    "test:visual": "npm run build && playwright test tests/e2e/visual-contracts.spec.js tests/e2e/visual-snapshots.spec.js",
    "test:print": "npm run build && playwright test tests/e2e/print-regression.spec.js",
    "test:ci": "npm run check && npm run test:unit && npm run test:e2e",
    "smoke:browser": "node scripts/smoke-browser-deployment.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/preact": "^3.2.4",
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "4.1.10",
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "preact": "^10.29.7"
  }
}
